# @package _global_
# one 96 GB GPU, all four suites, one epoch at global batch 128 (~37 h at 60 s/step; try batch_size=4 grad_accum=32)
task_name: libero_wamh3_1gpu
model:
  lora_r: 64
  lora_alpha: 64
  lora_adaln_r: 16
train:
  batch_size: 2
  grad_accum: 64
  num_workers: 8
  max_steps: 2200
  save_every: 100
  log_every: 1
